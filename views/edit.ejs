<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="flex justify-end">
        <a
          class="bg-red-500 px-3 py-2 rounded-md text-sm inline-block mb-5"
          href="/logout"
          >Logout</a
        >
      </div>
      <h5 class="mb-5">Edit Your Post</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 p-3 outline-none block mb-2 rounded-md overflow-hidden"
          name="content"
          placeholder="What's on your mind"><%= post.content %>
      </textarea>
        <button
          class="px-3 py-2 w-40 bg-yellow-500 text-black block rounded-md text-sm"
          type="submit"
        >
          Update Post
        </button>
      </form>
  </body>
</html>
